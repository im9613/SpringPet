package com.test.pet.mapper;

import com.test.pet.adoptDAO.AdoptReviewListDTO;
import com.test.pet.adoptDAO.List;
import com.test.pet.adoptDAO.Stringr;

public class AdoptReviewMapper {
	
	String getName(Stringr id);
	
	int add(AdoptReviewListDTO dto);
	
	List<AdoptReviewListDTO> list();
	
	AdoptReviewListDTO get(String seq);
	
	int edit(AdoptReviewListDTO dto);
	
	int del(AdoptReviewListDTO dto);

}
